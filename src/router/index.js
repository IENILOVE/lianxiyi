import VueRouter from 'vue-router'
import Vue from 'vue'

//1 通过Vue.use() 安装插件
Vue.use(VueRouter)


//2 创建VueRouter对象
const routers = [

]
const router = new VueRouter({
    routers
})


//3 将router对象传入到Vue实例
export default router